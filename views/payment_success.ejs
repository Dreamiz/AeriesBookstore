<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <title>Payment Success</title>
    <!-- Bootstrap CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light d-flex justify-content-center align-items-center vh-100">

    <div class="card shadow p-4" style="max-width: 450px; width: 100%;">
        <div class="card-body text-center">
            <h2 class="text-success mb-4"><%= payment.message %></h2>
            <p class="mb-2">Trạng thái: <strong><%= payment.status %></strong></p>
            <p class="mb-2">Sản phẩm: <%= payment.book %></p>
            <p class="mb-3">Số tiền: <strong><%= payment.amount %></strong> VNĐ</p>
            <a href="/" class="btn btn-primary mt-3">Quay lại trang chủ</a>
        </div>
    </div>

    <!-- Bootstrap JS Bundle (optional) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

